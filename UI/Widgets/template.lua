--- @type RCLootCouncil
local addon = select(2, ...)

local name = "TEMPLATE"
--- @class `name`: BackdropTemplate, Button, UI.embeds
local Object = {}

function Object:New(parent, name)

end

addon.UI:RegisterElement(Object, name)
